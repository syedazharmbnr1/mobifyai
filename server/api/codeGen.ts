import express from 'express';

const router = express.Router();

// Code generation routes - placeholder until implementation
router.get('/', (req, res) => {
  res.json({ message: 'Code Generation API - placeholder' });
});

export default router;